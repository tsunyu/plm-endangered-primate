# Python 3.8+
# Install: pip install -r analysis/requirements.txt
#
# ESM-2 (phase 4): install a PyTorch wheel matching your CUDA/CPU stack first, then transformers.
#   pip install torch --index-url https://download.pytorch.org/whl/cu118   # example CUDA 11.8
#   pip install transformers

# Core (figures, tables, and most phases)
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.4.0
seaborn>=0.11.0
pyyaml>=6.0
statsmodels>=0.13.0
scikit-learn>=1.0.0
openpyxl>=3.0.0

# Phase 2 ancestral FASTA access
pysam>=0.19.0

# Phase 4 ESM-2 only (large)
torch>=2.0.0
transformers>=4.30.0
